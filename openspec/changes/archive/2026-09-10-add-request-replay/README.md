# add-request-replay

Re-issue captured requests from the collector: resend as-is or edit method/url/headers/body in a modal (`POST /api/resend`); replays appear as first-class `replay` entries linked to the original
